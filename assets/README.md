# assets

Drop your mascot here as **`trash-panda.png`** — the README points at
`assets/trash-panda.png`.

Optional, matching set of artifact icons if you want to swap the emoji later:
`todo.png` · `shiny.png` · `plan.png` · `loot.png`.
